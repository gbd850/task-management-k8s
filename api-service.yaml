apiVersion: v1
kind: Service
metadata:
  name: task-management-backend-svc
spec:
  type: NodePort
  selector:
    app: task-management-backend
  ports:
    - protocol: "TCP"
      port: 8080 
      targetPort: 8080